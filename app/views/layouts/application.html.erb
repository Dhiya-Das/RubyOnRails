<!DOCTYPE html>
<html>
  <head>
    <title>Tasklist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <style>
.button {
  background-color: hsl(0, 100%, 70%);;
  border: none;
  color: white;
  padding: 15px 32px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
}
</style>
  </head>

  <body>
      <div class="button"> <%= link_to t('home'), root_path %> </div>
      <div class="button"> <%= link_to t('sign_in'), new_user_session_path %> </div>
      
      <% if user_signed_in? %>
      
      <div class="button"> <%= link_to t('edit'), edit_user_registration_path %> </div>
        
       <div class="button"> <%= link_to t('sign_out'), destroy_user_session_path, data: { turbo_method: :delete } %>  </div>
      <% end %>
    
       <div class="button">  <%= link_to t('sign_up'), new_user_registration_path %>  </div> 
      
      <div class="Enlang"> <%= link_to "English", root_path(locale: :en)%>
      <div class="Rulang"> <%= link_to "Russian", root_path(locale: :ru)%>   
      
     
    
     <p class="notice"><%= notice %></p>
     <p class="alert"><%= alert %></p>
      
    <%= yield %>
      
      

      
  </body>
</html>
